import styled from 'styled-components';

import { BLUE } from 'styles/colorConstants';

export const LabelBadge = styled.div`
  margin-bottom: 0.3rem;
  font-size: 1.2rem;
  text-align: center;
  color: ${BLUE};
`;
