import styled from 'styled-components';

import { BLACK, ORANGE } from 'styles/colorConstants';

export const StyledRequestList = styled.div``;

export const RequestsTitle = styled.h2`
  color: ${ORANGE};
  text-align: center;
`;

export const DefaultMessage = styled.h3`
  padding: 10px;
  color: ${BLACK};
  text-align: center;
`;
