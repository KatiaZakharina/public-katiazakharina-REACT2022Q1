import styled from 'styled-components';

import { ColumnContainer, StyledContainer } from 'components/Layout/Container';

export const StyledBookTour = styled(StyledContainer)`
  flex-wrap: wrap;
  gap: 0 30px;
  margin-bottom: 35px;
`;

export const FormInfo = styled(ColumnContainer)`
  margin-top: 48px;
  max-width: 550px;
`;
